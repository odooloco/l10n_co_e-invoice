para instalar la libreria gyp se debe de usar
pip o pip3 install git+https://chromium.googlesource.com/external/gyp

para instalar la libreria pycrypto==2.6.1 se debe de instalar primero
sudo apt-get install gcc

para instalar pygobject==3.32.2 se debe de instalar primero
apt-get install libcairo2-dev
apt-get install libgirepository1.0-dev

